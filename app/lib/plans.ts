/** Free tier: enforced when the shop has no active Pro subscription. */
export const FREE_MAX_IMAGES_PER_VARIANT = 6;
export const FREE_MAX_PRODUCTS_WITH_IMAGES = 5;

export const PRO_PLAN_DISPLAY = {
  name: "Pro",
  priceUsd: 9.99,
  interval: "month",
  trialDays: 7,
} as const;
